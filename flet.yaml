[build]
app_name = "Reticulum Viewer"
app_id = "com.reticulum.viewer"
version = "1.0.0"

[build.linux]
build_mode = "release"

[build.android]
build_mode = "release"
package_name = "reticulum_viewer"
version_code = 1
permissions = [
    "INTERNET",
    "ACCESS_NETWORK_STATE"
]
